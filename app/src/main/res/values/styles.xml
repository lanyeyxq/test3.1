<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 基础应用主题 -->
    <style name="Theme.MqttClient" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- 主色调 -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- 辅助色 -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- 窗口背景 -->
        <item name="android:windowBackground">@color/white</item>

        <!-- ActionBar样式 -->
        <item name="actionBarStyle">@style/Widget.MqttClient.ActionBar</item>
    </style>

    <!-- ActionBar样式 -->
    <style name="Widget.MqttClient.ActionBar" parent="Widget.MaterialComponents.ActionBar.Primary">
        <item name="titleTextStyle">@style/TextAppearance.MqttClient.ActionBar.Title</item>
    </style>

    <!-- ActionBar标题文字样式 -->
    <style name="TextAppearance.MqttClient.ActionBar.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>